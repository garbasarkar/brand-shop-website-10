const ErrorHandle = () => {
  return (
    <div className="mt-60">
      <h1 className="text-3xl font-semibold text-center">Oops!!!</h1>
      <p className="text-center mt-2">
        The requested URL /klds was not found on this server. <br /> That’s all
        we know.
      </p>
    </div>
  );
};

export default ErrorHandle;
